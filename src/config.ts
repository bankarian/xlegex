import { T } from "./types/type";

const config: T.GameConfig = {
  expectedLevelCount: 3,
  containerWidth: 400,
  containerHeight: 400,
  typeCount: 4,
  cardSize: 40,
  stackDepth: 10,
  unblockChances: 1,
  backChances: 1,
  matchCount: 3,
};

export default config;
