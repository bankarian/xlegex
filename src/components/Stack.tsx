import React from "react";
import config from "../config";
import { T } from "../types/type";

export const Stack: React.FC<T.CardNode[]> = (props) => {
  const { cardSize, stackDepth } = config;
  
  return <div></div>;
};
